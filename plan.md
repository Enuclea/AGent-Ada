1. **Add `yield_requested` check in `stream_agent_response`**: Check `yield_requested.get()` in the generator loops (`async for chunk in response.thoughts` and `async for chunk in response`). If it evaluates to True, `break` the loops to stop the main agent from generating further output or making another tool call in the same turn. This prevents "Ada" from spinning endlessly and forcing a handover.
2. **Handle Ping Event in UI**: Update the frontend script (`src/agent/static/app.js`) to parse and handle ping events (`{ type: "ping" }`). This keeps the `fetch` connection alive and indicates that the backend is healthy, even during long-running tasks.
3. **Change keep-alive line to Ping Event**: Update `src/agent/interfaces/web.py` to yield a properly formatted JSON ping event (`yield f"data: {json.dumps({'type': 'ping'})}\n\n"`) instead of the SSE comment (`yield ": keep-alive\n\n"`). This guarantees that the custom stream reader in `app.js` will see the data and not time out.
4. **Run Tests and verify**: Ensure that `app.js` doesn't throw errors when receiving the ping, and that `tools.py` successfully aborts generation. Run `pytest tests/` via `python -m pytest tests/` as specified in the memory guidelines.
5. **Submit changes**: Follow pre-commit steps.
